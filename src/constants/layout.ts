// Height of the custom bottom tab bar content, excluding the bottom safe-area
// inset. Shared so screens can offset above it (e.g. when lifting an input over
// the keyboard) without importing the navigator and creating a cycle.
export const TAB_BAR_HEIGHT = 64;

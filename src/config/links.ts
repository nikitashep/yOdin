// The published legal documents. They live on GitHub Pages, generated from the
// Markdown in docs/, so editing the file and merging updates what the app links
// to — there is nothing to ship in an app update.
//
// Play requires the privacy policy and the deletion page as store listing URLs,
// and the terms to be accepted in-app before a user can post anything.
const SITE = 'https://nikitashep.github.io/yOdin';

export const TERMS_URL = `${SITE}/terms-of-use/`;
export const PRIVACY_URL = `${SITE}/privacy-policy/`;
export const DELETE_ACCOUNT_URL = `${SITE}/delete-account/`;
